<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Form</title>
  </head>
  <body>
    <h1><%= title %></h1>
    <form action="/getForm" method="get">
      <h2>Get 요청</h2>
      <br />
      <input
        type="text"
        name="id"
        placeholder="ID는 최소 3글자"
        required
        minlength="3"
      />
      <input
        type="password"
        name="pw"
        placeholder="PW는 최소 4글자"
        required
        minlength="4"
      />
      <br />

      <input
        type="email"
        name="email"
        id="email"
        placeholder="email@EMAIL.com(필수)"
        required
      />
      <input type="number" name="grade" id="grade" placeholder="GRADE" />
      <button type="submit">제출</button>
    </form>

    <form action="/postForm" method="post">
      <!-- required : 폼 요소에 값이 반드시 들어가야하는지 여부를 결정 -->
      <!-- pattern : 폼 요소에 값이 입력되었을 때 입력값에 대한 검증의 의미 -->

      <h2>post 요청</h2>
      <br />
      <input
        type="text"
        name="id"
        placeholder="ID"
        pattern="^([a-zA-Z0-9가-힣]){4,}$"
        title="대소문자, 한글, 숫자로 구성된 4자 이상의 문자여야 해요"
        required
      />

      <input
        type="password"
        name="pw"
        placeholder="PW"
        pattern="^([a-z0-9A-Z]){8,12}$"
        title="대소문자인 영어와 숫자로 구성된 8-12자리이어야 합니다"
        required
      />
      <button type="submit">제출</button>
    </form>
  </body>
</html>
