<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>폼 전송 결과</title>

    <style>
      span {
        color: tomato;
        font-weight: 700;
      }
    </style>
  </head>

  <body>
    <h1><%= title %></h1>
    <div><span><%= userInfo.id %></span>님 환영합니다!</div>
    <div>비밀번호는 <span><%= userInfo.pw %></span>이세요!</div>

    <% if(userInfo.email) { %>
    <div>이메일은 <span><%= userInfo.email %></span>입니다</div>
    <% } %> <% if (userInfo.grade !== undefined && userInfo.grade !== '') { %>
    <div>학년은 <span><%= userInfo.grade %></span>학년 이군요!</div>
    <%}%>
  </body>
</html>

<!-- A && B
!A || !B -->
